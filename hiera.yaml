---
version: 5
defaults:
  datadir: "hieradata"
hierarchy:
  - name: "Normal data (unencrypted)"
    data_hash: yaml_data # Standard yaml backend
    paths:
      - "common.yaml"
      - "module.yaml"
